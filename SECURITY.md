# 安全政策

请不要在公开 issue 中报告密钥泄露、认证绕过、越权访问、Webhook 签名绕过、数据泄露或状态机绕过问题。

请通过仓库维护者的私有联系方式报告，并提供复现步骤、影响范围和必要的脱敏证据。维护者会确认问题、评估影响并协调修复。公开演示只可使用项目内置的模拟订单与演示 Provider。

部署时必须使用外部密钥管理系统注入 `LLM_API_KEY`、`FULFILLMENT_WEBHOOK_SECRET`、JWT 密钥和内部回调凭据；不要将这些值写入仓库、截图、Issue、日志或评测报告。
